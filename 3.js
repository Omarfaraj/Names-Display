var names = ['Faraj', 'Rashid', 'Zakariya', 'Halima', 'Abubakar'];

function displayNames() {
    var displayElement = document.GetElementById('nameDispaly');    

    displayElement = innerHTML = '';

    for (var i = 0; i < name.length; i++) {
    paragraph.textContext = names[i];

    displayElement.apendChild(paragraph);
    }
}

displayNames();
